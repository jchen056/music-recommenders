[pytest]
# Put the project root on sys.path so `from src.recommender import ...` works
# no matter how pytest is launched (plain `pytest`, `python -m pytest`, or from
# an IDE) or which directory it is run from.
pythonpath = .
testpaths = tests
